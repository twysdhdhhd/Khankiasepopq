{
  "name": "lottie-puppeteer",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "npx puppeteer install && apt-get update && apt-get install -y ffmpeg"
  },
  "dependencies": {
    "express": "^4.18.2",
    "fs-extra": "^11.1.1",
    "puppeteer": "^22.8.2",
    "uuid": "^9.0.0",
    "fluent-ffmpeg": "^2.1.2"
  },
  "license": "MIT"
}
